(def n (let (e (os/env "BENCH_N")
             v (if e (string/->number e) 5000000))
         (if (int? v) v (throw (str "BENCH_N is not an integer: " e)))))

;; higher-order combinator over a materialised range (0 .. n-1)
(io/puts (reduce (range n) 0 +))
