;; benchmark — a Brood project.
;; net/* ships in the default install as bundled std modules (ADR-097), so no
;; extra source path is needed to reach net/http.
(project
  :name "benchmark"
  :version "0.1.0"
  :source-paths ["src"])
